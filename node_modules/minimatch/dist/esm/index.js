import expand from 'brace-expansion';
import { assertValidPattern } from './assert-valid-pattern.js';
import { AST } from './ast.js';
import { escape } from './escape.js';
import { unescape } from './unescape.js';
export const minimatch = (p, pattern, options = {}) => {
    assertValidPattern(pattern);
    // shortcut: comments match nothing.
    if (!options.nocomment && pattern.charAt(0) === '#') {
        return false;
    }
    return new Minimatch(pattern, options).match(p);
};
// Optimized checking for the most common glob patterns.
const starDotExtRE = /^\*+([^+@!?\*\[\(]*)$/;
const starDotExtTest = (ext) => (f) => !f.startsWith('.') && f.endsWith(ext);
const starDotExtTestDot = (ext) => (f) => f.endsWith(ext);
const starDotExtTestNocase = (ext) => {
    ext = ext.toLowerCase();
    return (f) => !f.startsWith('.') && f.toLowerCase().endsWith(ext);
};
const starDotExtTestNocaseDot = (ext) => {
    ext = ext.toLowerCase();
    return (f) => f.toLowerCase().endsWith(ext);
};
const starDotStarRE = /^\*+\.\*+$/;
const starDotStarTest = (f) => !f.startsWith('.') && f.includes('.');
const starDotStarTestDot = (f) => f !== '.' && f !== '..' && f.includes('.');
const dotStarRE = /^\.\*+$/;
const dotStarTest = (f) => f !== '.' && f !== '..' && f.startsWith('.');
const starRE = /^\*+$/;
const starTest = (f) => f.length !== 0 && !f.startsWith('.');
const starTestDot = (f) => f.length !== 0 && f !== '.' && f !== '..';
const qmarksRE = /^\?+([^+@!?\*\[\(]*)?$/;
const qmarksTestNocase = ([$0, ext = '']) => {
    const noext = qmarksTestNoExt([$0]);
    if (!ext)
        return noext;
    ext = ext.toLowerCase();
    return (f) => noext(f) && f.toLowerCase().endsWith(ext);
};
const qmarksTestNocaseDot = ([$0, ext = '']) => {
    const noext = qmarksTestNoExtDot([$0]);
    if (!ext)
        return noext;
    ext = ext.toLowerCase();
    return (f) => noext(f) && f.toLowerCase().endsWith(ext);
};
const qmarksTestDot = ([$0, ext = '']) => {
    const noext = qmarksTestNoExtDot([$0]);
    return !ext ? noext : (f) => noext(f) && f.endsWith(ext);
};
const qmarksTest = ([$0, ext = '']) => {
    const noext = qmarksTestNoExt([$0]);
    return !ext ? noext : (f) => noext(f) && f.endsWith(ext);
};
const qmarksTestNoExt = ([$0]) => {
    const len = $0.length;
    return (f) => f.length === len && !f.startsWith('.');
};
const qmarksTestNoExtDot = ([$0]) => {
    const len = $0.length;
    return (f) => f.length === len && f !== '.' && f !== '..';
};
/* c8 ignore start */
const defaultPlatform = (typeof process === 'object' && process
    ? (typeof process.env === 'object' &&
        process.env &&
        process.env.__MINIMATCH_TESTING_PLATFORM__) ||
        process.platform
    : 'posix');
const path = {
    win32: { sep: '\\' },
    posix: { sep: '/' },
};
/* c8 ignore stop */
export const sep = defaultPlatform === 'win32' ? path.win32.sep : path.posix.sep;
minimatch.sep = sep;
export const GLOBSTAR = Symbol('globstar **');
minimatch.GLOBSTAR = GLOBSTAR;
// any single thing other than /
// don't need to escape / when using new RegExp()
const qmark = '[^/]';
// * => any number of characters
const star = qmark + '*?';
// ** when dots are allowed.  Anything goes, except .. and .
// not (^ or / followed by one or two dots followed by $ or /),
// followed by anything, any number of times.
const twoStarDot = '(?:(?!(?:\\/|^)(?:\\.{1,2})($|\\/)).)*?';
// not a ^ or / followed by a dot,
// followed by anything, any number of times.
const twoStarNoDot = '(?:(?!(?:\\/|^)\\.).)*?';
export const filter = (pattern, options = {}) => (p) => minimatch(p, pattern, options);
minimatch.filter = filter;
const ext = (a, b = {}) => Object.assign({}, a, b);
export const defaults = (def) => {
    if (!def || typeof def !== 'object' || !Object.keys(def).length) {
        return minimatch;
    }
    const orig = minimatch;
    const m = (p, pattern, options = {}) => orig(p, pattern, ext(def, options));
    return Object.assign(m, {
        Minimatch: class Minimatch extends orig.Minimatch {
            constructor(pattern, options = {}) {
                super(pattern, ext(def, options));
            }
            static defaults(options) {
                return orig.defaults(ext(def, options)).Minimatch;
            }
        },
        AST: class AST extends orig.AST {
            /* c8 ignore start */
            constructor(type, parent, options = {}) {
                super(type, parent, ext(def, options));
            }
            /* c8 ignore stop */
            static fromGlob(pattern, options = {}) {
                return orig.AST.fromGlob(pattern, ext(def, options));
            }
        },
        unescape: (s, options = {}) => orig.unescape(s, ext(def, options)),
        escape: (s, options = {}) => orig.escape(s, ext(def, options)),
        filter: (pattern, options = {}) => orig.filter(pattern, ext(def, options)),
        defaults: (options) => orig.defaults(ext(def, options)),
        makeRe: (pattern, options = {}) => orig.makeRe(pattern, ext(def, options)),
        braceExpand: (pattern, options = {}) => orig.braceExpand(pattern, ext(def, options)),
        match: (list, pattern, options = {}) => orig.match(list, pattern, ext(def, options)),
        sep: orig.sep,
        GLOBSTAR: GLOBSTAR,
    });
};
minimatch.defaults = defaults;
// Brace expansion:
// a{b,c}d -> abd acd
// a{b,}c -> abc ac
// a{0..3}d -> a0d a1d a2d a3d
// a{b,c{d,e}f}g -> abg acdfg acefg
// a{b,c}d{e,f}g -> abdeg acdeg abdeg abdfg
//
// Invalid sets are not expanded.
// a{2..}b -> a{2..}b
// a{b}c -> a{b}c
export const braceExpand = (pattern, options = {}) => {
    assertValidPattern(pattern);
    // Thanks to Yeting Li <https://github.com/yetingli> for
    // improving this regexp to avoid a ReDOS vulnerability.
    if (options.nobrace || !/\{(?:(?!\{).)*\}/.test(pattern)) {
        // shortcut. no need to expand.
        return [pattern];
    }
    return expand(pattern);
};
minimatch.braceExpand = braceExpand;
// parse a component of the expanded set.
// At this point, no pattern may contain "/" in it
// so we're going to return a 2d array, where each entry is the full
// pattern, split on '/', and then turned into a regular expression.
// A regexp is made at the end which joins each array with an
// escaped /, and another full one which joins each regexp with |.
//
// Following the lead of Bash 4.1, note that "**" only has special meaning
// when it is the *only* thing in a path portion.  Otherwise, any series
// of * is equivalent to a single *.  Globstar behavior is enabled by
// default, and can be disabled by setting options.noglobstar.
export const makeRe = (pattern, options = {}) => new Minimatch(pattern, options).makeRe();
minimatch.makeRe = makeRe;
export const match = (list, pattern, options = {}) => {
    const mm = new Minimatch(pattern, options);
    list = list.filter(f => mm.match(f));
    if (mm.options.nonull && !list.length) {
        list.push(pattern);
    }
    return list;
};
minimatch.match = match;
// replace stuff like \* with *
const globMagic = /[?*]|[+@!]\(.*?\)|\[|\]/;
const regExpEscape = (s) => s.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, '\\$&');
export class Minimatch {
    options;
    set;
    pattern;
    windowsPathsNoEscape;
    nonegate;
    negate;
    comment;
    empty;
    preserveMultipleSlashes;
    partial;
    globSet;
    globParts;
    nocase;
    isWindows;
    platform;
    windowsNoMagicRoot;
    regexp;
    constructor(pattern, options = {}) {
        assertValidPattern(pattern);
        options = options || {};
        this.options = options;
        this.pattern = pattern;
        this.platform = options.platform || defaultPlatform;
        this.isWindows = this.platform === 'win32';
        this.windowsPathsNoEscape =
            !!options.windowsPathsNoEscape || options.allowWindowsEscape === false;
        if (this.windowsPathsNoEscape) {
            this.pattern = this.pattern.replace(/\\/g, '/');
        }
        this.preserveMultipleSlashes = !!options.preserveMultipleSlashes;
        this.regexp = null;
        this.negate = false;
        this.nonegate = !!options.nonegate;
        this.comment = false;
        this.empty = false;
        this.partial = !!options.partial;
        this.nocase = !!this.options.nocase;
        this.windowsNoMagicRoot =
            options.windowsNoMagicRoot !== undefined
                ? options.windowsNoMagicRoot
                : !!(this.isWindows && this.nocase);
        this.globSet = [];
        this.globParts = [];
        this.set = [];
        // make the set of regexps etc.
        this.make();
    }
    hasMagic() {
        if (this.options.magicalBraces && this.set.length > 1) {
            return true;
        }
        for (const pattern of this.set) {
            for (const part of pattern) {
                if (typeof part !== 'string')
                    return true;
            }
        }
        return false;
    }
    debug(..._) { }
    make() {
        const pattern = this.pattern;
        const options = this.options;
        // empty patterns and comments match nothing.
        if (!options.nocomment && pattern.charAt(0) === '#') {
            this.comment = true;
            return;
        }
        if (!pattern) {
            this.empty = true;
            return;
        }
        // step 1: figure out negation, etc.
        this.parseNegate();
        // step 2: expand braces
        this.globSet = [...new Set(this.braceExpand())];
        if (options.debug) {
            this.debug = (...args) => console.error(...args);
        }
        this.debug(this.pattern, this.globSet);
        // step 3: now we have a set, so turn each one into a series of
        // path-portion matching patterns.
        // These will be regexps, except in the case of "**", which is
        // set to the GLOBSTAR object for globstar behavior,
        // and will not contain any / characters
        //
        // First, we preprocess to make the glob pattern sets a bit simpler
        // and deduped.  There are some perf-killing patterns that can cause
        // problems with a glob walk, but we can simplify them down a bit.
        const rawGlobParts = this.globSet.map(s => this.slashSplit(s));
        this.globParts = this.preprocess(rawGlobParts);
        this.debug(this.pattern, this.globParts);
        // glob --> regexps
        let set = this.globParts.map((s, _, __) => {
            if (this.isWindows && this.windowsNoMagicRoot) {
                // check if it's a drive or unc path.
                const isUNC = s[0] === '' &&
                    s[1] === '' &&
                    (s[2] === '?' || !globMagic.test(s[2])) &&
                    !globMagic.test(s[3]);
                const isDrive = /^[a-z]:/i.test(s[0]);
                if (isUNC) {
                    return [...s.slice(0, 4), ...s.slice(4).map(ss => this.parse(ss))];
                }
                else if (isDrive) {
                    return [s[0], ...s.slice(1).map(ss => this.parse(ss))];
                }
            }
            return s.map(ss => this.parse(ss));
        });
        this.debug(this.pattern, set);
        // filter out everything that didn't compile properly.
        this.set = set.filter(s => s.indexOf(false) === -1);
        // do not treat the ? in UNC paths as magic
        if (this.isWindows) {
            for (let i = 0; i < this.set.length; i++) {
                const p = this.set[i];
                if (p[0] === '' &&
                    p[1] === '' &&
                    this.globParts[i][2] === '?' &&
                    typeof p[3] === 'string' &&
                    /^[a-z]:$/i.test(p[3])) {
                    p[2] = '?';
                }
            }
        }
        this.debug(this.pattern, this.set);
    }
    // various transforms to equivalent pattern sets that are
    // faster to process in a filesystem walk.  The goal is to
    // eliminate what we can, and push all ** patterns as far
    // to the right as possible, even if it increases the number
    // of patterns that we have to process.
    preprocess(globParts) {
        // if we're not in globstar mode, then turn all ** into *
        if (this.options.noglobstar) {
            for (let i = 0; i < globParts.length; i++) {
                for (let j = 0; j < globParts[i].length; j++) {
                    if (globParts[i][j] === '**') {
                        globParts[i][j] = '*';
                    }
                }
            }
        }
        const { optimizationLevel = 1 } = this.options;
        if (optimizationLevel >= 2) {
            // aggressive optimization for the purpose of fs walking
            globParts = this.firstPhasePreProcess(globParts);
            globParts = this.secondPhasePreProcess(globParts);
        }
        else if (optimizationLevel >= 1) {
            // just basic optimizations to remove some .. parts
            globParts = this.levelOneOptimize(globParts);
        }
        else {
            // just collapse multiple ** portions into one
            globParts = this.adjascentGlobstarOptimize(globParts);
        }
        return globParts;
    }
    // just get rid of adjascent ** portions
    adjascentGlobstarOptimize(globParts) {
        return globParts.map(parts => {
            let gs = -1;
            while (-1 !== (gs = parts.indexOf('**', gs + 1))) {
                let i = gs;
                while (parts[i + 1] === '**') {
                    i++;
                }
                if (i !== gs) {
                    parts.splice(gs, i - gs);
                }
            }
            return parts;
        });
    }
    // get rid of adjascent ** and resolve .. portions
    levelOneOptimize(globParts) {
        return globParts.map(parts => {
            parts = parts.reduce((set, part) => {
                const prev = set[set.length - 1];
                if (part === '**' && prev === '**') {
                    return set;
                }
                if (part === '..') {
                    if (prev && prev !== '..' && prev !== '.' && prev !== '**') {
                        set.pop();
                        return set;
                    }
                }
                set.push(part);
                return set;
            }, []);
            return parts.length === 0 ? [''] : parts;
        });
    }
    levelTwoFileOptimize(parts) {
        if (!Array.isArray(parts)) {
            parts = this.slashSplit(parts);
        }
        let didSomething = false;
        do {
            didSomething = false;
            // <pre>/<e>/<rest> -> <pre>/<rest>
            if (!this.preserveMultipleSlashes) {
                for (let i = 1; i < parts.length - 1; i++) {
                    const p = parts[i];
                    // don't squeeze out UNC patterns
                    if (i === 1 && p === '' && parts[0] === '')
                        continue;
                    if (p === '.' || p === '') {
                        didSomething = true;
                        parts.splice(i, 1);
                        i--;
                    }
                }
                if (parts[0] === '.' &&
                    parts.length === 2 &&
                    (parts[1] === '.' || parts[1] === '')) {
                    didSomething = true;
                    parts.pop();
                }
            }
            // <pre>/<p>/../<rest> -> <pre>/<rest>
            let dd = 0;
            while (-1 !== (dd = parts.indexOf('..', dd + 1))) {
                const p = parts[dd - 1];
                if (p && p !== '.' && p !== '..' && p !== '**') {
                    didSomething = true;
                    parts.splice(dd - 1, 2);
                    dd -= 2;
                }
            }
        } while (didSomething);
        return parts.length === 0 ? [''] : parts;
    }
    // First phase: single-pattern processing
    // <pre> is 1 or more portions
    // <rest> is 1 or more portions
    // <p> is any portion other than ., .., '', or **
    // <e> is . or ''
    //
    // **/.. is *brutal* for filesystem walking performance, because
    // it effectively resets the recursive walk each time it occurs,
    // and ** cannot be reduced out by a .. pattern part like a regexp
    // or most strings (other than .., ., and '') can be.
    //
    // <pre>/**/../<p>/<p>/<rest> -> {<pre>/../<p>/<p>/<rest>,<pre>/**/<p>/<p>/<rest>}
    // <pre>/<e>/<rest> -> <pre>/<rest>
    // <pre>/<p>/../<rest> -> <pre>/<rest>
    // **/**/<rest> -> **/<rest>
    //
    // **/*/<rest> -> */**/<rest> <== not valid because ** doesn't follow
    // this WOULD be allowed if ** did follow symlinks, or * didn't
    firstPhasePreProcess(globParts) {
        let didSomething = false;
        do {
            didSomething = false;
            // <pre>/**/../<p>/<p>/<rest> -> {<pre>/../<p>/<p>/<rest>,<pre>/**/<p>/<p>/<rest>}
            for (let parts of globParts) {
                let gs = -1;
                while (-1 !== (gs = parts.indexOf('**', gs + 1))) {
                    let gss = gs;
                    while (parts[gss + 1] === '**') {
                        // <pre>/**/**/<rest> -> <pre>/**/<rest>
                        gss++;
                    }
                    // eg, if gs is 2 and gss is 4, that means we have 3 **
                    // parts, and can remove 2 of them.
                    if (gss > gs) {
                        parts.splice(gs + 1, gss - gs);
                    }
                    let next = parts[gs + 1];
                    const p = parts[gs + 2];
                    const p2 = parts[gs + 3];
                    if (next !== '..')
                        continue;
                    if (!p ||
                        p === '.' ||
                        p === '..' ||
                        !p2 ||
                        p2 === '.' ||
                        p2 === '..') {
                        continue;
                    }
                    didSomething = true;
                    // edit parts in place, and push the new one
                    parts.splice(gs, 1);
                    const other = parts.slice(0);
                    other[gs] = '**';
                    globParts.push(other);
                    gs--;
                }
                // <pre>/<e>/<rest> -> <pre>/<rest>
                if (!this.preserveMultipleSlashes) {
                    for (let i = 1; i < parts.length - 1; i++) {
                        const p = parts[i];
                        // don't squeeze out UNC patterns
                        if (i === 1 && p === '' && parts[0] === '')
                            continue;
                        if (p === '.' || p === '') {
                            didSomething = true;
                            parts.splice(i, 1);
                            i--;
                        }
                    }
                    if (parts[0] === '.' &&
                        parts.length === 2 &&
                        (parts[1] === '.' || parts[1] === '')) {
                        didSomething = true;
                        parts.pop();
                    }
                }
                // <pre>/<p>/../<rest> -> <pre>/<rest>
                let dd = 0;
                while (-1 !== (dd = parts.indexOf('..', dd + 1))) {
                    const p = parts[dd - 1];
                    if (p && p !== '.' && p !== '..' && p !== '**') {
                        didSomething = true;
                        const needDot = dd === 1 && parts[dd + 1] === '**';
                        const splin = needDot ? ['.'] : [];
                        parts.splice(dd - 1, 2, ...splin);
                        if (parts.length === 0)
                            parts.push('');
                        dd -= 2;
                    }
                }
            }
        } while (didSomething);
        return globParts;
    }
    // second phase: multi-pattern dedupes
    // {<pre>/*/<rest>,<pre>/<p>/<rest>} -> <pre>/*/<rest>
    // {<pre>/<rest>,<pre>/<rest>} -> <pre>/<rest>
    // {<pre>/**/<rest>,<pre>/<rest>} -> <pre>/**/<rest>
    //
    // {<pre>/**/<rest>,<pre>/**/<p>/<rest>} -> <pre>/**/<rest>
    // ^-- not valid because ** doens't follow symlinks
    secondPhasePreProcess(globParts) {
        for (let i = 0; i < globParts.length - 1; i++) {
            for (let j = i + 1; j < globParts.length; j++) {
                const matched = this.partsMatch({
  "en":{
    "language": "English",
    "localized": "English",
    "professional": true,
    "translators":[
      {
        "credit": "The AdBlock Team",
        "username": null,
        "professional": true,
        "proofreader": true,
        "joined": "2009-12"
      }
    ]
  },
  "ar": {
    "language": "Arabic",
    "localized": "Ø§Ù„Ø¹Ø±Ø¨ÙŠØ©",
    "professional": false,
    "translators": [
      {
        "credit": "Ahmed Walid",
        "username": "ahmedwalid05",
        "professional": false,
        "proofreader": false,
        "joined": "2013-12"
      },
      {
        "credit": "Umar Hoj",
        "username": "uhoj999",
        "professional": false,
        "proofreader": false,
        "joined": "2013-08"
      },
      {
        "credit": "2B-4G10",
        "username": "2B-4G10",
        "professional": false,
        "proofreader": false,
        "joined": "2015-10"
      },
      {
        "credit": "ABDELGHANI HOURANI",
        "username": "abdelghani.me",
        "professional": false,
        "proofreader": false,
        "joined": "2020-04"
      },
      {
        "credit": "Khaled Hosny",
        "username": "khaledhosny",
        "professional": false,
        "proofreader": false,
        "joined": "2019-01"
      },
      {
        "credit": "Mumtaz76",
        "username": "Mumtaz76",
        "professional": false,
        "proofreader": false,
        "joined": "2016-02"
      },
      {
        "credit": "mmona",
        "username": "mmona",
        "professional": false,
        "proofreader": false,
        "joined": "2017-09"
      },
      {
        "credit": "Safa Alfulaij",
        "username": "safaalfulaij",
        "professional": false,
        "proofreader": false,
        "joined": "2017-08"
      },
      {
        "credit": "yazan.abohethm",
        "username": "yazan.abohethm",
        "professional": false,
        "proofreader": false,
        "joined": "2019-12"
      },
      {
        "credit": "Ahmed",
        "username": "FiberAhmed",
        "professional": false,
        "proofreader": true,
        "joined": "2021-11"
      },
      {
        "credit": "yngams",
        "username": "younessmouhid",
        "professional": false,
        "proofreader": false,
        "joined": "2022-01"
      }
    ]
  },
  "bg":{
    "language": "Bulgarian",
    "localized": "Ğ±ÑŠĞ»Ğ³Ğ°Ñ€ÑĞºĞ¸",
    "professional": false,
    "translators":[
      {
        "credit": "ĞĞ¸ĞºĞ¾Ğ»Ğ°Ğ¹ Ğ¤Ğ¸Ğ»Ğ¸Ğ¿Ğ¾Ğ²",
        "username": "zmeYpc",
        "professional": false,
        "proofreader": true,
        "joined": "2011-05"
      },
      {
        "credit": "djradimix",
        "username": "djradimix",
        "professional": false,
        "proofreader": false,
        "joined": "2020-01"
      }
    ]
  },
  "ca":{
    "language": "Catalan",
    "localized": "catalÃ ",
    "professional": false,
    "translators":[
      {
        "credit": "CatTranslations",
        "username": "CatTranslations",
        "professional": false,
        "proofreader": false,
        "joined": "2013-10"
      },
      {
        "credit": "Jaime MuÃ±oz MartÃ­n",
        "username": "jmmartin_5",
        "professional": false,
        "proofreader": true,
        "joined": "2016-04"
      }
    ]
  },
  "cs":{
    "language": "Czech",
    "localized": "ÄeÅ¡tina",
    "professional": false,
    "translators":[
      {
        "credit": "Petr CÃ­zl",
        "username": null,
        "professional": false,
        "proofreader": false,
        "joined": "2010-09"
      },
      {
        "credit": "Viktor De Pasquale",
        "username": "neytireyqa",
        "professional": false,
        "proofreader": false,
        "joined": "2015-01"
      },
      {
        "credit": "TomÃ¡Å¡ Branda",
        "username": "PapuleX",
        "professional": false,
        "proofreader": false,
        "joined": "2017-08"
      },
      {
        "credit": "Dominio Suzi",
        "username": "DominioCz",
        "professional": false,
        "proofreader": false,
        "joined": "2014-07"
      },
      {
    and reduce . and .. portions in the file as well.
        // dont' need to do the second phase, because it's only one string[]
        const { optimizationLevel = 1 } = this.options;
        if (optimizationLevel >= 2) {
            file = this.levelTwoFileOptimize(file);
        }
        this.debug('matchOne', this, { file, pattern });
        this.debug('matchOne', file.length, pattern.length);
        for (var fi = 0, pi = 0, fl = file.length, pl = pattern.length; fi < fl && pi < pl; fi++, pi++) {
            this.debug('matchOne loop');
            var p = pattern[pi];
            var f = file[fi];
            this.debug(pattern, p, f);
            // should be impossible.
            // some invalid regexp stuff in the set.
            /* c8 ignore start */
            if (p === false) {
                return false;
            }
            /* c8 ignore stop */
            if (p === GLOBSTAR) {
                this.debug('GLOBSTAR', [pattern, p, f]);
                // "**"
                // a/**/b/**/c would match the following:
                // a/b/x/y/z/c
                // a/x/y/z/b/c
                // a/b/x/b/x/c
                // a/b/c
                // To do this, take the rest of the pattern after
                // the **, and see if it would match the file remainder.
                // If so, return success.
                // If not, the ** "swallows" a segment, and try again.
                // This is recursively awful.
                //
                // a/**/b/**/c matching a/b/x/y/z/c
                // - a matches a
                // - doublestar
                //   - matchOne(b/x/y/z/c, b/**/c)
                //     - b matches b
                //     - doublestar
                //       - matchOne(x/y/z/c, c) -> no
                //       - matchOne(y/z/c, c) -> no
                //       - matchOne(z/c, c) -> no
                //       - matchOne(c, c) yes, hit
                var fr = fi;
                var pr = pi + 1;
                if (pr === pl) {
                    this.debug('** at the end');
                    // a ** at the end will just swallow the rest.
                    // We have found a match.
                    // however, it will not swallow /.x, unless
                    // options.dot is set.
                    // . and .. are *never* matched by **, for explosively
                    // exponential reasons.
                    for (; fi < fl; fi++) {
                        if (file[fi] === '.' ||
                            file[fi] === '..' ||
                            (!options.dot && file[fi].charAt(0) === '.'))
                            return false;
                    }
                    return true;
                }
                // ok, let's see if we can swallow whatever we can.
                while (fr < fl) {
                    var swallowee = file[fr];
                    this.debug('\nglobstar while', file, fr, pattern, pr, swallowee);
                    // XXX remove this slice.  Just pass the start index.
                    if (this.matchOne(file.slice(fr), pattern.slice(pr), partial)) {
                        this.debug('globstar found match!', fr, fl, swallowee);
                        // found a match.
                        return true;
                    }
                    else {
                        // can't swallow "." or ".." ever.
                        // can only swallow ".foo" when explicitly asked.
                        if (swallowee === '.' ||
                            swallowee === '..' ||
                            (!options.dot && swallowee.charAt(0) === '.')) {
                            this.debug('dot detected!', file, fr, pattern, pr);
                            break;
                        }
                        // ** swallows a segment, and continue.
                        this.debug('globstar swallow a segment, and continue');
                        fr++;
                    }
                }
                // no match was found.
                // However, in partial mode, we can't say this is necessarily over.
                /* c8 ignore start */
                if (partial) {
                    // ran out of file
                    this.debug('\n>>> no match, partial?', file, fr, pattern, pr);
                    if (fr === fl) {
                        return true;
                    }
                }
                /* c8 ignore stop */
                return false;
            }
            // something other than **
            // non-magic patterns just have to match exactly
            // patterns with magic have been turned into regexps.
            let hit;
            if (typeof p === 'string') {
                hit = f === p;
                this.debug('string match', p, f, hit);
            }
            else {
                hit = p.test(f);
                this.debug('pattern match', p, f, hit);
            }
            if (!hit)
                return false;
        }
        // Note: ending in / means that we'll get a final ""
        // at the end of the pattern.  This can only match a
        // corresponding "" at the end of the file.
        // If the file ends in /, then it can only match a
        // a pattern that ends in /, unless the pattern just
        // doesn't have any more for it. But, a/b/ should *not*
        // match "a/b/*", even though "" matches against the
        // [^/]*? pattern, except in partial mode, where it might
        // simply not be reached yet.
        // However, a/b/ should still satisfy a/*
        // now either we fell off the end of the pattern, or we're done.
        if (fi === fl && pi === pl) {
            // ran out of pattern and filename at the same time.
            // an exact hit!
            return true;
        }
        else if (fi === fl) {
            // ran out of file, but still had pattern left.
            // this is ok if we're doing the match as part of
            // a glob fs traversal.
            return partial;
        }
        else if (pi === pl) {
            // ran out of pattern, still have file left.
            // this is only acceptable if we're on the very last
            // empty segment of a file with a trailing slash.
            // a/* should match a/b/
            return fi === fl - 1 && file[fi] === '';
            /* c8 ignore start */
        }
        else {
            // should be unreachable.
            throw new Error('wtf?');
        }
        /* c8 ignore stop */
    }
    braceExpand() {
        return braceExpand(this.pattern, this.options);
    }
    parse(pattern) {
        assertValidPattern(pattern);
        const options = this.options;
        // shortcuts
        if (pattern === '**')
            return GLOBSTAR;
        if (pattern === '')
            return '';
        // far and away, the most common glob pattern parts are
        // *, *.*, and *.<ext>  Add a fast check method for those.
        let m;
        let fastTest = null;
        if ((m = pattern.match(starRE))) {
            fastTest = options.dot ? starTestDot : starTest;
        }
        else if ((m = pattern.match(starDotExtRE))) {
            fastTest = (options.nocase
                ? options.dot
                    ? starDotExtTestNocaseDot
                    : starDotExtTestNocase
                : options.dot
                    ? starDotExtTestDot
                    : starDotExtTest)(m[1]);
        }
        else if ((m = pattern.match(qmarksRE))) {
            fastTest = (options.nocase
                ? options.dot
                    ? qmarksTestNocaseDot
                    : qmarksTestNocase
                : options.dot
                    ? qmarksTestDot
                    : qmarksTest)(m);
        }
        else if ((m = pattern.match(starDotStarRE))) {
            fastTest = options.dot ? starDotStarTestDot : starDotStarTest;
        }
        else if ((m = pattern.match(dotStarRE))) {
            fastTest = dotStarTest;
        }
        const re = AST.fromGlob(pattern, this.options).toMMPattern();
        if (fastTest && typeof re === 'object') {
            // Avoids overrS  ½   I;Ît@„it€yrH‹IUL),  èÜãÿÿ3ÛHT$`!\$XE3ÀH!\$`H‹Î3ÿHÿ}&  D  …ÀuZHÿõ%  D  ‹ØH‹—S  I;Æ„>  @„h„4  @8h‚*  HÿÃ%  D  H‹gS  L‹Î‰D$ H‹IèK÷ÿÿé  H‹L$`HD$XE3ÉH‰D$ E3À‹ÕHÿì%  D  …ÀuTHÿt%  D  ‹ØƒøztAH‹S  I;Î„¸   @„i„®   @8i‚¤   H‹IL9+  º   D‹Èè€ãÿÿé‡   ‹L$Xè’ùÿÿH‹øH…ÀuXëqD‹L$XHD$XH‹L$`L‹Ç‹ÕH‰D$ HÿY%  D  …ÀuHHÿá$  D  ‹ØH‹ƒR  I;Ît$@„it@8irH‹IL·*  º   D‹ÈèşâÿÿH‹ÏèÒùÿÿ3ÿ‹ËHÿŸ$  D  H‹L$`H…ÉtSHÿÑ$  D  …ÀuCH‹!R  I;Æt7@„ht1@8hr+HÿY$  D  H‹ıQ  LF*  D‹Èº   H‹Iè‰âÿÿH‹\$PH‹ÇH‹l$hHƒÄ0A^_^ÃÌÌÌÌÌÌÌH‰\$H‰t$H‰|$ AVHìP  H‹ÁQ  H3ÄH‰„$@  H‹ñ3ÒHL$ A¸  èê  H‹‚Q  L5{Q  I;Ît!öAt€yrH‹IL¯)  º   è]áÿÿHL$ è'ûÿÿ3Û…ÀuRHÿ#  D  ‹øH‹0Q  I;Ît"öAt€yrH‹ISD‹ÈL^)  è­áÿÿÿÄ  u‰é¢   ‹Çé   HL$ èÉüÿÿH‹øH…Àu@Hÿ*#  D  ‹ØH‹ÌP  I;Ît"öAt€yrH‹IWD‹ÈLú(  èIáÿÿ‹ÃëKö@|t4H‹”P  I;Ît-öAt'€yr!H‹ILL$ º   L¾(  èàÿÿë»   H‹Ï‰èØ÷ÿÿ3ÀH‹Œ$@  H3Ìèö  Lœ$P  I‹[I‹s I‹{(I‹ãA^ÃÌÌÌÌÌÌÌÌH‹ÄH‰XH‰pH‰x H‰PUATAUHh¡HìÀ   3ÒHM·A¸ˆ   èQ  3öH‹çO  L%àO  L-)(  I;ÌtöAt€yrH‹IVM‹ÅèÁßÿÿHMoèèıÿÿ‹Ø…Àu	9uo…s  èì  …À„Ù   A¹ H?'  E3ÀèôÿÿH‹øH…Àt}Hh'  H‹ÈèÔôÿÿ…Àt0H‹]O  I;ÌtöAt€yrH‹Iº   M‹ÅèCßÿÿ¾   ë)H‹-O  I;ÌtöAt€yrH‹Iº   M‹ÅèßÿÿH‹ÏHÿy   D  ë?H‹óN  I;Ät3ö@t-€xr'Hÿ+!  D  H‹ÏN  º   D‹ÈM‹ÅH‹Iè_ßÿÿHÜ%  HÍ&  H‰U¿H‰EÏLE·H&  H‰E×H¿&  H‰EÿHÌ&  H‰Eõ@@  ‰E‹Æ÷ØHÉHƒeß H#Êº   H‰MÇ3ÉHÿ!  D  H‹øH…ÀuKHÿ‘   D  ‹ØH‹3N  I;ÌtöAt€yrH‹IWD‹ÈM‹Åè´Şÿÿƒû…×   » éÍ   èH  …Àtm…ötiHƒe§ LE§E3ÉÇE¯   H‹ÏAQHÿ   D  …Àu?H‹¿M  I;Ät3ö@t-€xr'Hÿ÷  D  H‹›M  º    D‹ÈM‹ÅH‹Iè+ŞÿÿH‹ÏHÿ   D  …Àu?H‹mM  I;Ät3ö@t-€xr'Hÿ¥  D  H‹IM  º!   D‹ÈM‹ÅH‹IèÙİÿÿè(   ‹ÃLœ$À   I‹[ I‹s0I‹{8I‹ãA]A\]ÃÌÌÌÌÌÌÌÌÌ@SHƒì Hƒd$0 Hƒd$8 H‹ïL  HèL  H;Èt!öAt€yrH‹IL%  º%   èÊÜÿÿHL$8Hÿ>  D  ‹Ø…ÀxeLD$0º   3ÉHÿ  D  ‹Ø…Àx5L‹D$0E3Éº 0  ¹    Hÿ  D  H‹L$8H‹T$0H‹H‹@(ÿ˜  H‹L$8H‹H‹@ÿ†  ‹ÃHƒÄ [ÃÌÌÌÌÌÌÌÌÌÌH‰\$H‰l$WHƒì H‹L  H-L  H;ÍtNöAt"€yrH‹ILK$  º<   èùÛÿÿH‹êK  H;Ít!öAt€yrH‹ILF%  º%   èÌÛÿÿA¹ Ho#  E3Àè3ğÿÿH‹ØH…ÀuMHÿğ  D  H‹”K  H;Ít"öAt€yrH‹IS&D‹ÈLê$  èÜÿÿ3ÀH‹\$8H‹l$@HƒÄ _ÃÌH£$  H‹Ëè·ğÿÿH‹Ë‹øHÿ·  D  H‹3K  H;Ít5öAt/€yr)H‹IH"  …ÿLv$  º'   Lz$  LEÈè-Ûÿÿ…ÿtˆHL$0è#ùÿÿ…Àt4H‹àJ  H;Ít$öAt€yrH‹IL#  º=   D‹Èè[Ûÿÿ3Àë‹D$0…À…:ÿÿÿè~úÿÿ‹Ø…Àt0H‹•J  H;Ít$öAt€yrH‹ILÉ"  º>   D‹ÈèÛÿÿ‹ÃéúşÿÿÌÌÌÌÌÌÌÌÌH‹ÄH‰HH‰PL‰@L‰H USWH¨ˆøÿÿHì`  H‹IJ  H3ÄH‰…P  H‹Ù3ÒHL$PA¸   ès  3ÿ9=P  …®   HD$@A¹  E3ÀH‰D$ HJ"  HÇÁ  €Hÿ  D  …Àt‹Çë`H‹L$@HD$4H‰D$(LL$8HD$0ÇD$4   E3ÀH‰D$ H?"  Hÿ  D  …Àuƒ|$8t‰|$0H‹L$@Hÿğ  D  ‹D$0H‹€  ‰’I  ÇhO     ë‹€I  …À›   L‹ÃLˆ  »ÿ  HL$P‹ÓHÿ;  D  …ÀxuH˜H;Ãwnuf‰½N  HL$PHƒÈÿHÿÀf9<Au÷ºı  A¸
   H;ÂBĞBÿfD9DDPt&AHf‰LTPBJfD‰DDPHÉHù   s1f‰|PHL$PHÿ’  D  H‹P  H3ÌèN
  HÄ`  _[]ÃÌè%  ÌÌÌÌÌÌÌÌÌ@WHìP  H‹ŒH  H3ÄH‰„$@  3ÒHL$$A¸  è¸  H‹PH  H=IH  H;Ït!öAt€yrH‹IL¥!  º
   è+ØÿÿHL$ ÇD$   Hÿ÷  D  3Ò…ÀuQH‹ıG  H;Ç„›   ö@„‘   €x‚‡   Hÿ)  D  H‹ÍG  L>!  D‹Èº   H‹IèYØÿÿëZƒ|$0tH‹¥G  H;ÏtGöAtA€yr;º   ë$ƒ|$$s4H‹G  H;Ït!öAt€yrº   H‹ILÖ   èa×ÿÿ3Àé‚   ŠŒ$8  ¸@   „Èt€¼$:  HàDÁëb¸   f…„$8  t¸   ëLöÁt¸   ë@¸€   „Èt¸   ë0¹   f…Œ$8  u!ŠŒ$:  €ùu¸   ë€é¸   €ùGÂH‹Œ$@  H3Ìèp  HÄP  _ÃÌÌÌÌÌÌÌH‰\$WHƒì H‹ŸF  H=˜F  H;Ït!öAt€yrH‹ILô  º   èzÖÿÿ3ÛHT$0‰\$0èN   …Àx	9\$0•Ãë-H‹NF  H;Ït!öAt€yrH‹ILª  º   è0Öÿÿ‹ÃH‹\$8HƒÄ _ÃÌÌÌÌÌÌÌH‰\$H‰t$H‰|$AVHƒì H‹ò3ÛH‹óE  L5ìE  I;ÎtöAt€yrH‹ISLE  èĞÕÿÿH…öu
¸W €éã   èü   …À„Ê   HÅ  Hÿş  D  H‹øH…Àt{Hº  H‹ÈHÿ@  D  H…Àu=Hÿ¿  D  ‹ØH‹aE  I;Ît5öAt/€yr)H‹IL½  º   èCÕÿÿëH‹ÖHï  ÿi  ‹ØH‹ÏHÿí  D  ë>H‹E  I;Ît!öAt€yrH‹ILk  º   èñÔÿÿ»…  ë
èYüÿÿƒà\‰‹ÃH‹\$0H‹t$8H‹|$@HƒÄ A^ÃÌÌÌÌÌÌÌÌ@WHìP  H‹ÀD  H3ÄH‰„$@  H‹–D  H=D  H;Ït!öAt€yrH‹ILû  º   èqÔÿÿ3ÒHL$$A¸  è¸  HL$ ÇD$   Hÿ+  D  3É…ÀuGH‹1D  H;Çt7ö@t1€xr+Hÿi  D  H‹D  L  D‹Èº   // short-circuit in the case of busted things.
        // comments, etc.
        if (this.comment) {
            return false;
        }
        if (this.empty) {
            return f === '';
        }
        if (f === '/' && partial) {
            return true;
        }
        const options = this.options;
        // windows: need to use /, not \
        if (this.isWindows) {
            f = f.split('\\').join('/');
        }
        // treat the test path as a set of pathparts.
        const ff = this.slashSplit(f);
        this.debug(this.pattern, 'split', ff);
        // just ONE of the pattern sets in this.set needs to match
        // in order for it to be valid.  If negating, then just one
        // match means that we have failed.
        // Either way, return on the first hit.
        const set = this.set;
        this.debug(this.pattern, 'set', set);
        // Find the basename of the path by looking for the last non-empty segment
        let filename = ff[ff.length - 1];
        if (!filename) {
            for (let i = ff.length - 2; !filename && i >= 0; i--) {
                filename = ff[i];
            }
        }
        for (let i = 0; i < set.length; i++) {
            const pattern = set[i];
            let file = ff;
            if (options.matchBase && pattern.length === 1) {
                file = [filename];
            }
            const hit = this.matchOne(file, pattern, partial);
            if (hit) {
                if (options.flipNegate) {
                    return true;
                }
                return !this.negate;
            }
        }
        // didn't get any hits.  this is success if it's a negative
        // pattern, failure otherwise.
        if (options.flipNegate) {
            return false;
        }
        return this.negate;
    }
    static defaults(def) {
        return minimatch.defaults(def).Minimatch;
    }
}
/* c8 ignore start */
export { AST } from './ast.js';
export { escape } from './escape.js';
export { unescape } from './unescape.js';
/* c8 ignore stop */
minimatch.AST = AST;
minimatch.Minimatch = Minimatch;
minimatch.escape = escape;
minimatch.unescape = unescape;
//# sourceMappingURL=index.js.map